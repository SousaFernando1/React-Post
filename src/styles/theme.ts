import { colors } from './colors';
import { fonts } from './fonts';
import { shadows } from './shadows';
import { sizes } from './sizes';

export default {
  COLORS: colors,
  SHADOWS: shadows,
  FONTS: fonts,
  SIZES: sizes,
};
